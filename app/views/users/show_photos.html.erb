<% @microposts.each do |micropost| %>
	<%= image_tag micropost.media.url, class: :micropost__img if micropost.has_image? %>
  <%= video_tag micropost.media.url, class: :micropost__video, controls: true if micropost.has_video? %>
<% end %>